Thumbs.db
Desktop.ini
.DS_Store
*.log
.env
.env.local
node_modules/
scripts/calendar-feeds.config.json
scripts/pricelabs-feeds.config.json

# Ephemeral commit-message scratch file used to build git commit -F on PowerShell
# (which doesn't heredoc). Should never be committed.
.tmp-commit-msg.txt

# Private / research artifacts — never commit
# Personal Gmail exports (customer service / support archives) containing PII
images/lodging/[[]#*[]] Collections*
images/lodging/*Gmail.html
images/lodging/*Gmail.pdf
images/lodging/*Gmail_files/
